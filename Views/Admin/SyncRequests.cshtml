@{
    ViewBag.Title = "Sync Requests";
    ViewBag.ActiveMenu = "SyncRequests";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="requests-management">
    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-striped" id="requestsTable">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>User</th>
                            <th>Company</th>
                            <th>Tool</th>
                            <th>External ID</th>
                            <th>Status</th>
                            <th>Created</th>
                            <th>Updated</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
            <div id="pagination" class="pagination-wrapper mt-3"></div>
        </div>
    </div>
</div>

<!-- Process Request Modal -->
<div class="modal fade" id="processRequestModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Process Request</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body">
                <form id="processRequestForm">
                    <input type="hidden" id="processRequestId" />
                    
                    <div class="mb-3">
                        <label class="form-label">External Sync ID</label>
                        <input type="number" class="form-control" id="externalSyncId" required />
                        <div class="form-text">Enter the ID from the external system.</div>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Outcome</label>
                        <select class="form-select" id="processOutcome" required>
                            <option value="true">Successful</option>
                            <option value="false">Failed</option>
                        </select>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-primary" id="saveProcessBtn">Save Changes</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/Scripts/Admin/SyncRequests.js"></script>
}